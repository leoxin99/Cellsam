# A2 交接记录 #001 (2026-03-07 20:52)

## 1. 本窗口完成
- A1 已审核 `docs/agent_inbox.md` 中 A2 的 `paper_preparation.md` 更新方案。
- 已确认 T27a 当前训练配置应以 `src/config/t27a_planb_decoder.yaml` 为准，不再用旧的 Phase1 / Best Config 充当当前主线。
- 已确认 T31 论文口径 baseline 应使用 `Cellpose v3.1.1 + diameter=250`。
- 已确认 T34 Arm C 旧数值已失效，最新有效结果见 `experiments/t34_official_path_ablation/results_val.json`。
- A1 已把审计结论写回 `docs/agent_inbox.md`，供 R1 汇总。

## 2. 进行中任务
- 更新 `docs/paper_preparation.md`：
  - `§2.2` 改为当前 T27a loss 主配置
  - `§2.4` 增加 `F1 (=RQ)` 的简洁说明
  - `§3.1` 更新主表与 split 标注
  - `§3.5` 补时间线
- 处理 T34 在论文中的放置方式：
  - `val71` 新结果可写入方法学敏感性/ablation
  - `test73` 未补跑前，不要把 T34 写成最终 test 结论

## 3. 下一窗口第一步
1. 读 `CLAUDE.md` 了解当前总状态。
2. 读 `docs/agent_inbox.md` 最新 5 条，尤其是 A1 于 `2026-03-07 20:52` 的审计回执。
3. 读 `docs/paper_preparation.md`，按审计要求更新 `§2.2`, `§2.4`, `§3.1`, `§3.5`。
4. 读 `docs/experiments/completed/T27a_planb_decoder_bf.md`、`docs/experiments/active/T31_cellpose_paper_aligned.md`、`docs/experiments/active/T34_t27a_official_path_ablation.md`。
5. 若要写 T34，必须以 `experiments/t34_official_path_ablation/results_val.json` 为当前有效证据，不得再回写旧 Arm C `0.447`。

## 4. 风险与未决事项
- `paper_preparation.md` 当前主表标题还是 `Oracle, test73`，不要把 `val71` 行无标注混入。
- `Precision/Recall` 只对有 `TP/FP/FN` 的实验补；历史实验拿不到就保留 `RQ (=F1)` 或填 `—`。
- `IoU Head MSE` 可以写，但应明确它是 `train.py` 里额外加到总 loss 的训练项，不是 `CombinedLoss` 内部组件。
- `T18` 若仍为 single-seed，继续标注 `provisional` / `single-seed`。

## 5. 关键证据/产物
- 代码:
  - `src/config/t27a_planb_decoder.yaml`
  - `src/train.py`
- 结果:
  - `experiments/t34_official_path_ablation/results_val.json`
- 文档:
  - `docs/paper_preparation.md`
  - `docs/experiments/completed/T27a_planb_decoder_bf.md`
  - `docs/experiments/active/T31_cellpose_paper_aligned.md`
  - `docs/experiments/active/T34_t27a_official_path_ablation.md`
  - `docs/agent_inbox.md`
